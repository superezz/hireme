name: ViewMe CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-lint-client:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./client

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js 18.x
      uses: actions/setup-node@v3
      with:
        node-version: 18.x
        cache: 'npm'
        cache-dependency-path: './client/package-lock.json'
        
    - name: Install dependencies
      run: npm ci

    - name: Run ESLint
      run: npm run lint

    - name: Build frontend
      run: npm run build
      
  # Note: Automated backend tests do not exist yet.
  # This section can be enabled once tests are written.
  # build-and-test-server:
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./server
  #   steps:
  #   - uses: actions/checkout@v3
  #   - name: Use Node.js 18.x
  #     uses: actions/setup-node@v3
  #     with:
  #       node-version: 18.x
  #       cache: 'npm'
  #       cache-dependency-path: './server/package-lock.json'
  #   - name: Install dependencies
  #     run: npm ci
  #   - name: Run tests
  #     run: npm test
